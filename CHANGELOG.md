# Changelog

## [2.7.0] - 2026-06-30

### ✨ 新增功能

- **从属性提取标题和描述**：发布时自动从 Markdown frontmatter 中提取标题和描述，填充到发布表单。可在设置中开启（默认关闭），属性名可自定义（默认 `title` 和 `description`）
- **发布摘要支持**：发布弹窗新增描述输入框，内容同步到微信草稿的 `digest` 字段（可选，120 字以内）
- **外部链接转脚注**：预览、复制、发布时自动将 `[文本](https://url)` 外部链接转为脚注格式（文本 + 上标编号 + 文末 URL 列表），不修改源文件
- **内部链接转纯文本**：`[[内部链接]]` 自动转为纯文本，去除链接标记

### 🎨 优化

- **发布弹窗重构**：精简布局，去除冗余分割线，左右 30%/70% 对齐，封面图预览区域扩大，遵循 Obsidian 主题色变量

### 🐛 Bug 修复

- **链接转脚注保留子元素**：链接内的图片、加粗等内联格式不再丢失
- **脚注样式兼容微信**：脚注分隔线和文字颜色使用硬编码值，不再依赖 Obsidian CSS 变量（微信环境无法解析）
- **重复链接去重**：同一 URL 多次引用时复用脚注编号
- **链接转脚注异常处理**：添加 try-catch，异常时输出错误日志而非静默失败

---

## [2.6.2] - 2026-06-24

### 🔧 代码规范

- 最低支持版本调整为 1.12.0（Obsidian 1.13 尚未发布）
- 微信 API 响应定义精确接口类型，替代 `any` 直接访问
- `JSON.parse` 结果添加类型标注（素材缓存、Token 缓存、sessionStorage 数据）
- `catch (error)` 改为 `catch (error: unknown)`，用 `instanceof Error` 替代直接访问属性
- Logger 方法参数 `any[]` → `unknown[]`
- `setCssProps` 使用 camelCase 属性名（`pointerEvents`）
- Promise 调用加 `void` 前缀，避免未处理警告
- 移除 `asWechatResponse` 辅助函数（含禁止的 eslint-disable）
- 移除未使用的 `parseCssString` 导入
- `text-indent: 0px` → `text-indent: 0`（兼容性）
- README 补充英文安装说明和使用指南
- ConfirmModal 支持 async onConfirm 回调

---

## [2.6.1] - 2026-06-24

### 🔧 代码规范

- 最低支持版本提升至 1.13.0
- 使用 Obsidian 规范 API：`setCssProps` 替代直接 style 赋值、`sanitizeHTMLToDom` 替代 innerHTML、`activeDocument` 替代 `document`、`window.setTimeout` 替代 `setTimeout`、`RequestUrlResponse` 替代 `any` 类型
- 移除不允许的 eslint-disable 注释，用 `window.fetch` 替代全局 `fetch`、用精确类型替代 `any`
- 定义 `LeafWithUpdateHeader` 接口替代 `as any` 访问内部 API
- async 回调改为 `void (async () => { ... })()` 模式，避免 Promise 未处理
- 移除 await 非 Promise 的调用
- enum 比较使用枚举值替代字符串字面量
- CSS `!important` 替换为更高选择器特异性
- `builtin-modules` 依赖替换为 Node.js 内置 `module.builtinModules`
- README 添加英文标题和描述

### 🐛 Bug 修复

- CSS 内联失败时显示提示，不再静默忽略
- 列表转换增加迭代上限防护，避免极端嵌套场景下无限循环

### 🎨 优化

- 列表处理逻辑统一为共享函数，复制和发布流程保持一致

---

## [2.5.4] - 2026-06-23

### 🐛 Bug 修复

- **代码块缩进全面保留**：代码块中**所有**普通空格（U+0020）替换为不间断空格（U+00A0），不仅行首缩进空格，还包括行内的对齐空格、key-value 之间的空格等，确保 JSON/YAML/Python 等代码在公众号中缩进完整保留

---

## [2.5.3] - 2026-06-17

### 🐛 Bug 修复

- **Callout 边框样式**：Callout 的 border-left 不再写入内联样式，主题 CSS 可以通过 juice 正确内联控制
- **列表缩进重构**：列表缩进改为由容器 `.mp-list-section` 的 `padding-left` 控制，顶层 1em、嵌套层固定 1.5em
- **代码块缩进保留**：代码块行首空格替换为不间断空格（U+00A0），公众号中不再折叠丢失缩进
- **发布流程行内代码**：发布时正文中的行内代码转为 `<span>` 标签，避免微信 API 将其渲染为独立代码块
- **发布流程代码块换行**：发布时代码块换行处理与复制流程对齐
- **发布流程本地图片**：本地图片优先用 fetch 读取（和复制流程保持一致），避免路径解析失败导致图片丢失

---

## [2.5.2] - 2026-05-15

### 🐛 Bug 修复

- **使用指南/更新日志加载修复**：修复非开发环境安装插件后，使用指南和更新日志无法加载的问题

---

## [2.5.1] - 2026-05-14

### ✨ 新增功能

- **多公众号支持**：支持配置最多 3 个微信公众号账号，发布时可选择目标账号
- **账号独立管理**：每个公众号使用独立的 Token 和素材库，互不干扰

### 🎨 优化

- **设置页面重新设计**：更清爽简洁的设置界面
- **AppID 输入容错**：自动去除输入前后的空格，避免因空格导致发布失败

---

## [2.4.6] - 2026-05-12

### 🎨 优化

- **代码规范优化**：修复 Obsidian 社区插件审核 Bot 提出的代码规范问题
- **主题预览隔离**：修复主题管理页面预览和侧边栏预览样式互相影响的问题

---

## [2.4.5] - 2026-05-11

### ✨ 新增功能

- **查看更新**：设置页新增「查看更新」入口，直接查看版本更新记录

### 🎨 优化

- **使用指南排版优化**：优化使用指南页面的标题间距、列表缩进、视频圆角等排版细节

### 🐛 Bug 修复

- **本地图片复制修复**：修复复制到公众号后图片无法粘贴到后台的问题

---

## [2.4.4] - 2026-05-09

### ✨ 新增功能

- **社区投稿主题**：新增社区投稿主题分区，内置「缤纷多彩」和「微信绿」两个社区主题，作者名可点击跳转
- **快速切换控制**：主题卡片增加勾选框，控制该主题是否出现在预览界面的快速切换列表中
- **使用指南**：设置页新增「使用指南」入口，查看插件功能介绍和使用说明

### 🎨 优化

- **主题管理排序**：分区顺序调整为自定义 → 社区投稿 → 内置
- **操作按钮常驻**：主题卡片的操作按钮（预览、代码、删除）改为常驻显示
- **使用说明折叠区**：主题管理页面顶部增加可折叠的使用说明

### 🐛 Bug 修复

- **外部图片复制修复**：修复包含外部 HTTP 图片的文章复制时报 SecurityError 的问题

---

## [2.4.2] - 2026-04-21

### 🐛 Bug 修复

- **发布代码块换行修复**：发布到公众号时代码块内容正确换行显示
- **发布代码高亮修复**：发布到公众号时代码块语法高亮颜色正确显示

---

## [2.4.1] - 2026-04-16

### 🎨 主题管理界面重构

#### 新增

- **主题预览独立视图**：独立窗口查看主题排版效果，支持切换不同主题实时对比
- **CSS 代码查看视图**：语法高亮显示主题 CSS，自定义主题支持在线编辑
- **投稿主题**：一键跳转 GitHub Issue 提交自定义主题

#### 优化

- **交互简化**：点击卡片整行切换主题，操作按钮改为 hover 显示的 icon
- **视图复用**：预览和代码视图复用已有窗口，不重复打开
- **视觉升级**：间距节奏、按钮层级、选中状态全面优化

---

## [2.3.7] - 2026-04-16

### ✨ 新增功能

- **Mermaid 图表支持**：流程图、甘特图等 Mermaid 图表复制/发布到公众号后正常显示

### 🐛 Bug 修复

- **数学公式显示优化**：行内公式图片大小与正文保持一致，不再撑开行距
- **目录链接显示**：Obsidian 目录链接（`[[#标题]]`）复制后正常显示为纯文本

---

## [2.3.6] - 2026-04-10

### 🐛 Bug 修复

- **修复列表缩进和空行问题**：列表统一使用 section 标签，修复二级缩进不显示和列表项间多余空行
- **修复嵌套列表层级丢失**：嵌套列表递归时正确计算缩进层级

---

## [2.3.5] - 2026-04-07

### 🐛 Bug 修复

#### 代码块样式复制到公众号后丢失 (#27)
- **修复代码高亮丢失**：复制/发布到公众号时，代码块的语法高亮颜色现在能正确保留
- **修复仿 macOS 窗口圆点不显示**

---

## [2.3.4] - 2026-03-20

### 🐛 Bug 修复

#### 列表显示问题
- **列表转换重构**：完全避免微信公众号对列表元素的自动处理
- **修复编号错乱**：有序列表编号不再被微信后台重新计算
- **修复嵌套列表**：嵌套列表顺序和缩进正确显示
- **修复多余空行**：列表前后不再出现额外空行

---

## [2.3.3] - 2026-03-20

### ✨ 新增功能

#### 数学公式支持
- **LaTeX 公式渲染**：支持在 Markdown 中使用 LaTeX 数学公式，发布时自动转换为 PNG 图片
- **行内公式**：支持 `$...$` 和 `\(...\)` 语法
- **块级公式**：支持 `$$...$$` 和 `\[...\]` 语法
- **在线渲染**：使用 CodeCogs API 渲染公式，确保微信公众号兼容
- **设置开关**：在插件设置中可开启/关闭数学公式转换功能
- **复制支持**：复制到公众号时也支持数学公式转换

#### 主题管理
- **自定义主题重命名**：支持对本地自定义主题进行二次修改名称

---

## [2.3.1] - 2026-03-16

### ✨ 新增功能

#### 发布进度指示器
- **右上角进度显示**：发布时在页面右上角显示实时进度条，清晰展示发布进度
- **图片上传进度**：逐张显示正在处理的图片名称和进度（X/Y）
- **状态反馈**：
  - 处理中：蓝色进度条 + 脉冲动画 + 闪烁效果
  - 成功：绿色进度条，显示"发布成功！"
  - 失败：红色进度条，显示具体错误信息
- **防关闭提示**：底部显示"⚠️ 请勿关闭此页面"警告
- **响应式设计**：支持移动端适配

### 🚀 性能优化

#### 构建优化
- **移除调试文件**：生产构建不再生成 `${filename}__assets` 文件夹和 JSON 文件
- **禁用 Sourcemap**：生产版本不生成 sourcemap 文件，减小构建体积
- **纯净输出**：只生成 `main.js` 必需文件

### 🐛 Bug 修复

#### 修复
- **发布按钮状态**：发布完成后正确重置按钮状态，避免卡住

### 📝 技术细节

#### 新增文件
- `src/ui/ProgressIndicator.ts` - 进度指示器组件

#### 修改文件
- `src/publisher/wechat.ts` - 集成进度指示器到发布流程
- `src/ui/modals.ts` - 优化发布按钮状态处理
- `esbuild.config.mjs` - 优化构建配置
- `styles.css` - 添加进度指示器样式

---

## [2.2.6] - 2026-03-07

### 🐛 Bug 修复

#### 修复
- **复制/发布样式跨设备不一致**：复制到剪贴板和发布到公众号时，使用 juice 将 CSS 内联到每个元素的 `style` 属性上，确保在任何设备上粘贴或发布后样式都能正确显示
- **列表项多出空行**：发布时无序列表和有序列表项之间出现无效空行，将列表项内部包裹元素从块级 `<section>` 改为行内 `<span>`
- **自定义主题目录路径错误**：本地自定义主题目录使用 `manifest.dir` 获取插件实际安装路径，不再硬编码路径导致在错误位置创建 `custom` 文件夹

---

## [2.2.5] - 2026-03-06

### 🐛 Bug 修复

#### 修复
- **复制粘贴跨设备样式丢失**：复制到剪贴板时，CSS 依赖 `<style>` 标签而非内联 `style` 属性，导致在不同电脑/浏览器上粘贴到公众号后台时样式不显示。现在复制时通过 juice 将 CSS 内联到每个元素的 `style` 属性上
- **发布到公众号样式丢失**：发布时直接克隆预览 DOM 序列化，`<style>` 标签被公众号后台剥离导致样式全部丢失。改为统一使用 `markdownToHtml()` 生成带内联样式的 HTML

---

## [2.2.4] - 2026-03-05

### 📦 Callout 支持(beta) & 代码清理

#### 新增
- **Callout 提示框支持(beta)**：在预览中正确显示 Obsidian 的 30+ 种 Callout 类型（note/tip/warning/danger/success/example/todo/quote 等）
- 每种 Callout 类型有对应的 emoji 图标、背景色、边框色和标题色
- 所有 8 个内置主题 CSS 均添加 Callout 预览样式，暗色主题有专属配色
- Callout 使用内联样式，为后续公众号发布兼容做准备

#### 移除
- 移除自定义字体管理功能（添加/删除自定义字体的 UI 和 `addCustomFont`/`removeCustomFont`/`updateCustomFont` 方法）
- 主题管理界面字体区域简化为仅展示内置字体列表

#### 文档
- 更新 README 安装方式：BRAT 插件安装（推荐）+ 手动从 Release 下载
- 完善功能说明、预览控件说明、使用技巧、FAQ

---

## [2.1.3] - 2026-03-05

### 🔧 主题管理优化

#### 新增
- **查看 CSS 源码**：主题卡片新增「查看 CSS」按钮，展开查看完整 CSS 内容
- **复制 CSS**：查看 CSS 时可一键复制完整主题 CSS 到剪贴板
- **刷新预览按钮**：预览视图左上角新增🔄刷新按钮，强制重新加载本地主题和预览

#### 修复
- 主题管理界面切换主题后，预览视图不同步的问题
- 自定义主题在预览页面主题选择器中不显示的问题
- 编辑自定义主题 CSS 后预览不更新的问题

#### 优化
- 预览视图主题选择器排序调整为：本地自定义主题 → 内置主题
- 主题同步机制增强：支持主题 ID 变更、主题列表变更、CSS 内容变更三重检测
- 移除锁定预览功能，文档编辑时始终实时预览

---

## [2.1.2] - 2026-03-05

### 🎨 主题系统重构

整个样式配置系统从 JSON 模板迁移到**纯 CSS 主题系统**，参考 mdnice 的实现方案，更灵活、更易扩展。

#### 新增
- **纯 CSS 主题系统**：使用 `.mp-content-section` 作为根选择器，通过标准 CSS 文件定义主题样式
- **8 个内置 CSS 主题**：默认、优雅、暗色、极简、清新绿、暖橙、猩红、学术
- **本地自定义主题**：支持在插件目录 `custom/` 文件夹下放置 `.css` 文件作为自定义主题
- **主题管理界面**：通过命令面板「打开主题管理」打开独立的主题管理视图，支持浏览、切换、新建、编辑、删除主题
- **CSS 在线编辑器**：在主题管理界面中直接编写和保存自定义 CSS 主题
- **字体管理**：在主题管理界面中统一管理内置和自定义字体

#### 变更
- **设置页面精简**：设置页面只保留微信公众号 API 配置，主题和样式管理移至独立界面
- **样式内联方式**：发布时使用 [juice](https://github.com/Automattic/juice) 库将 CSS 内联到 HTML，替代旧的手动样式拼接
- **预览视图简化**：移除背景选择器，保留主题选择器、字体选择器和字号调整

#### 移除
- 旧的 JSON 模板系统（11 个 JSON 模板文件）
- 模板管理器（`templateManager`）和背景管理器（`backgroundManager`）
- 创建模板/背景/字体的独立弹窗
- 旧的 `WechatStyler` 样式处理器

---

## [2.0.0] - 2026-03-04

### 新增
- 微信公众号一键发布功能
- 实时预览窗口
- 12+ 预设主题模板
- 图片自动上传到微信服务器
- 草稿同步与更新
- 封面图选择（素材库 / 本地上传）
- 元数据管理（发布状态、图片映射）
- 一键复制格式化内容到剪贴板
- 代码块语法高亮
- MathJax 公式转 SVG
- Mermaid 图表转 PNG

---

## [1.0.0] - 2026-02-05

### 🎉 首次发布

整合 [MP Preview](https://github.com/Yeban8090/mp-preview) 和 [Enhanced Publisher](https://github.com/chararch/obsidian-enhanced-publisher) 两个开源项目，实现**自定义主题 + 一键发布到公众号后台**的完整工作流。

#### 项目背景

每次从 Obsidian 写完内容都要手动复制到公众号后台，然后再调整格式，这个过程经常耗费很长时间。搜索后发现两个符合需求的开源项目：

- **MP Preview**：Markdown 内容转公众号格式预览，可以自定义主题，但不能发布
- **Enhanced Publisher**：支持直接发布 Markdown 内容到公众号后台，但不能自定义主题

本插件将两者结合，实现了**能自定义主题且能一键发布到公众号后台**的完整方案。

#### 功能
- 自定义样式主题预览
- 一键发布到微信公众号草稿箱
- 图片自动上传到微信服务器
- 实时预览公众号效果
- 一键复制格式化内容到剪贴板
- 多种预设主题模板
- 字体和字号调整

#### 致谢
- [MP Preview](https://github.com/Yeban8090/mp-preview) - 样式模板预览系统
- [Enhanced Publisher](https://github.com/chararch/obsidian-enhanced-publisher) - 微信发布功能
